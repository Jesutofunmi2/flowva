import { createContext } from "react";

export const RequestLoader = createContext({});
export const ToastContext = createContext(null);
export const MetaHandler = createContext({});
export const platformHandler = createContext({});
